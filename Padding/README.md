概要

パディングの実装例
構造体にするときにバイト数が異なる型を置くと自動的にパディングが起きます。
例えば、usinged charとintを同じ構造体に置くと
usinged charのあとに3バイト分のパディングがおきる。
しかし、同じ型名しかない場合はパディングが発生しない

なのでもしパディングが発生したら
内部構造では構造体を扱って
送信直前に配列に別の配列に置いてから送信する(padding_send.cを参照)